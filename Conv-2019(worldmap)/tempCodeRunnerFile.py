world_data = {}

# print(data.country_zn_en)

# for country_data in json.loads(requests.post(url).text)['data']:
#     world_data[data.country_zn_en[country_data['name']]] = country_data['confirm']

# world_data_list = list(world_data.items())